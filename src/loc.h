#pragma once
#include <windows.h>

// Локализация. 0 = русский (встроенный, по умолчанию). 1 = English (файл lang\en.lang,
// генерируется при первом переключении; формат «русский<TAB>перевод» — редактируемый).
void LocSetLang(int lang);            // переключить, при необходимости сгенерировать/прочитать файл, сохранить выбор
int  LocGetLang();                    // текущий язык (читается из реестра при старте)

// Перевод по русскому ключу: для русского вернёт сам ключ, иначе — перевод из файла (или ключ, если нет).
const wchar_t* T(const wchar_t* ru);

// Перевести заголовок диалога и подписи всех его контролов (по их русскому тексту). Для русского — ничего.
void LocDialog(HWND dlg);
